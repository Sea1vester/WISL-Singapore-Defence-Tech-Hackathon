services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: sdth-telemetry/platform-api/Dockerfile
    ports:
      - "0.0.0.0:8000:8000"
    environment:
      DATABASE_PATH: /data/telemetry.db
      REDIS_URL: redis://redis:6379/0
      INGEST_API_KEYS: ${INGEST_API_KEYS:-dev-key-12345}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:7b}
      RAW_UPLOAD_DIR: /data/raw-uploads
      VISUALS_DIR: /data/visuals
      RAW_DATASETS_DIR: /datasets
      REPLAY_STATIC_DIR: /replay/public
      DEMO_STATIC_DIR: /demo
      REDACT_OPERATOR_LOCATION: ${REDACT_OPERATOR_LOCATION:-true}
      RETENTION_DAYS: ${RETENTION_DAYS:-30}
    volumes:
      - sqlite-data:/data
      - ../raw_telemetry-datasets:/datasets:ro
      - ./platform-api/app:/app/app:ro
      - ../sdth-replay/public:/replay/public:ro
      - ../sdth-replay/src:/replay/src:ro
      - ../sdth-demo:/demo:ro
    depends_on:
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

  worker:
    build:
      context: ..
      dockerfile: sdth-telemetry/platform-api/Dockerfile
    command: ["python", "-m", "app.worker"]
    environment:
      DATABASE_PATH: /data/telemetry.db
      REDIS_URL: redis://redis:6379/0
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-deepseek-r1:7b}
      RAW_UPLOAD_DIR: /data/raw-uploads
      VISUALS_DIR: /data/visuals
      REDACT_OPERATOR_LOCATION: ${REDACT_OPERATOR_LOCATION:-true}
      RETENTION_DAYS: ${RETENTION_DAYS:-30}
    volumes:
      - sqlite-data:/data
      - ./platform-api/app:/app/app:ro
    depends_on:
      redis:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  sqlite-data:
